apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redacta-development
  namespace: argocd
spec:
  project: redacta-development
  source:
    repoURL: git@github.com:babasoyturan/Redacta.git
    targetRevision: dev
    path: deploy/helm/redacta
    helm:
      releaseName: redacta
      valueFiles:
        - values-cloud-development.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: redacta
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
